version: '3.8'
services:
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ScooterDB
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3309:3306"